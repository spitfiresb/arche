.env
.env.*
!.env.example

# Cloudflare Pages local dev secrets (wrangler reads this for `pages dev`)
.dev.vars

# Wrangler local dev cache
.wrangler/

.DS_Store

# Python bytecode from the dashboard data scripts
__pycache__/
*.pyc

# Local static server (no-store headers so edits show without a hard reload)
.devserver.py
